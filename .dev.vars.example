# Copy to .dev.vars for local Pages Functions testing (wrangler pages dev
# reads this automatically). In production this lives in the Cloudflare
# Pages project's environment variables instead — never commit the real key.

STRIPE_SECRET_KEY=sk_test_...
