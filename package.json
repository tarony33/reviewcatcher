{
  "name": "reviewcatcher-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "preview:functions": "npm run build && wrangler pages dev ./dist"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "@astrojs/sitemap": "^3.7.3"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
